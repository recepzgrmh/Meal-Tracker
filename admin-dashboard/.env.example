# Copy to .env.local and fill in. Both values ship to the browser, so only ever
# put the publishable (anon) key here — never the service-role key.
#
# The dev project's values live in ../config/app_config.dev.json.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
